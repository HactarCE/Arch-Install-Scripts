#!/usr/bin/env zsh

source header.sh

echo "${bold}If you want to use a display manager other than LightDM, install it yourself and run ${blue}systemctl enable ${yellow}display-manager-service${white}. If you want LightDM, run ${yellow}223a1-lightdm${white}.${nofmt}"
