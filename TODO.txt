xorg-drivers ?
add README

more --noconfirm on pacman

efibootmgr like this:
efibootmgr -c -d /dev/sda -p 1 -L "Arch Linux" -l "EFI\arch\grubx64.efi" -v

do os-prober, additional grub-mkconfig, and efibootmgr after booting in

use chfn to change user details

ttf-ubuntu-font-family is required for lighdm-slick-greeter, not lightdm

keyboard layout
