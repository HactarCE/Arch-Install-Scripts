#!/usr/bin/env zsh

source header.sh

echo "${bold}The ${yellow}1xx-${white} scripts should be run from a live Arch USB.${nofmt}"
echo
echo "${bold}Most of these scripts require ${blue}zsh${white}. You are currently using ${yellow}${SHELL}${white}. If that looks like it's ${blue}zsh${white}, then you're good to go!${nofmt}"
