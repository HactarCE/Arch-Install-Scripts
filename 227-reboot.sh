#!/usr/bin/env zsh

source header.sh

echo "${bold}It's a good idea to ${blue}reboot${white} and log in to make sure your graphics drivers, display manager, and desktop environment/window manager are working. You can continue the installation from there or in a TTY. Either way, you'll want to be logged in as a ${underline}normal, non-root user${no_underline} for all subsequent commands.${nofmt}"
